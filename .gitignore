# 掲載用の下書き。冒頭に自分用の方針メモが入っており、公開すると発注者に読まれる。
# このリポジトリは GitHub Pages で全世界に配信されるので、絶対にコミットしないこと。
実績まとめ_下書き.txt

.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
node_modules/

# 素材の実際の置き場所と、実名の置き換え表。公開すると「誰の案件か」が分かってしまう。
# 末尾の * は .bak / .orig などの控えを巻き込むため（2026-08-26 に .bak が
# 追跡対象になりかけた。表そのものが漏れるので、拡張子違いも必ず除外する）
tools/sources.local.json*
*.bak
*.orig

# 案件の帳票（内部用）。2026-09-08 に、案件カード.txt が公開されていたことに気づいて追加した
# （コミット 30be9ff で追跡対象に入り、GitHub Pages の URL で誰でも読める状態だった）。
# 中に「次の1手」やローカルの置き場所が書いてあり、実績まとめ_下書き.txt と同じ理由で公開しない。
案件カード.txt
【要対応】*
【プロンプト】*
【手作業】*
